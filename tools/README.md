# WonderQuest Tools

This folder contains local generation scripts for the WonderQuest build.

- `build_planning_workbook.py`: generates the first planning workbook from the launch JSON files

Use this folder for build-time helpers only.
